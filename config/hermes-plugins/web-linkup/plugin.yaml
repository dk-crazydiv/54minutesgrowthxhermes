name: web-linkup
version: 1.0.0
description: "Linkup web search (https://www.linkup.so). Requires LINKUP_API_KEY in ~/.hermes/.env; falls back to the built-in ddgs provider while the key is unset, so search never breaks."
author: 54minutes
kind: backend
provides_web_providers:
  - linkup
